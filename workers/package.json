{
  "name": "headless-markets-event-indexer",
  "version": "1.0.0",
  "description": "Cloudflare Worker: Base L2 event indexer for Headless Markets",
  "main": "event-indexer.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv:namespace create INDEXER_STATE"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}